import 'package:flutter/material.dart';

class settingTabWidget extends StatelessWidget {
  const settingTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }
}
