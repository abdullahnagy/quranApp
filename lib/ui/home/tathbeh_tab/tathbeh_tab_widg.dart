import 'package:flutter/material.dart';

class tathbehTabWidget extends StatelessWidget {
  const tathbehTabWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.deepOrange,
    );
  }
}
